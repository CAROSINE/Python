a = (1,) # to create tuple with single element must ase , after the value else pylance will think its a int
print(type(a))  # a(1,2,3,45, False, "Rohan") tuple with multiple element
                # no way to change tuple
