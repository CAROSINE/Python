cities = ["Dhaka", "Chittagong", "Khulna", "Barishal"]

cities.reverse() # reverse() is used to reverse the order of the elements in the list.
print(cities) # we can also use several pop() to remove several elements from the list.
length = len(cities)
print(length) # len() is used to find the length of the list. It returns the number of elements in the list.